apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-nfs
  namespace: nfs-controller
spec:
  interval: 10m
  chart:
    spec:
      chart: csi-driver-nfs
      version: "4.12.0"
      sourceRef:
        kind: HelmRepository
        name: csi-driver-nfs
        namespace: nfs-controller
      interval: 12h
