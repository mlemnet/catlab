apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-data-pvc
  namespace: linkding
spec:
  storageClassName: nfs-client-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
